{{-- <div class="modal fade" id="{{ $id ?? 'customModal' }}" tabindex="-1"
  aria-labelledby="{{ $label ?? 'customModalLabel' }}" aria-hidden="true">
  <div class="modal-dialog {{ $size ?? 'modal-md' }}">
    <div class="modal-content rounded-4 shadow">

      <div class="modal-header bg-primary text-white rounded-top">
        <h5 class="modal-title fw-semibold" id="{{ $label ?? 'customModalLabel' }}">
          {{ $title ?? 'Modal Title' }}
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

      <div class="modal-body">
        {{ $body ?? 'Modal content goes here.' }}
      </div>

      @isset($footer)
      <div class="modal-footer">
        {!! $footer !!}
      </div>
      @endisset

    </div>
  </div>
</div> --}}